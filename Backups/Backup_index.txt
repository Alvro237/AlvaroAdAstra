<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>My Aerospace Portfolio</title>
  <meta name="description" content="Personal site + aerospace mini-tools" />
  <!--
    You can keep everything in one file while learning. We'll later split this
    into index.html, styles.css, and app.js.
  -->
  <style>
    :root{
      --bg: #0b1020;
      --bg-soft: #121933;
      --text: #e7eaf6;
      --muted: #b7c0d8;
      --accent: #5eead4; /* teal */
      --accent-2: #93c5fd; /* blue */
      --card: #0f152b;
      --border: #26304f;
    }
    html{scroll-behavior:smooth}
    body{
      margin:0; font: 16px/1.6 system-ui, -apple-system, Segoe UI, Roboto, Ubuntu, Cantarell, Noto Sans, Arial, "Apple Color Emoji", "Segoe UI Emoji";
      background: var(--bg); color: var(--text);
    }
    .container{max-width: 980px; margin: 0 auto; padding: 0 1.2rem}

    header{
      position: sticky; top:0; z-index:10; backdrop-filter: blur(6px);
      background: color-mix(in oklab, var(--bg), transparent 40%);
      border-bottom: 1px solid var(--border);
    }
    .nav{display:flex; align-items:center; justify-content:space-between; padding: .8rem 0}
    .logo{font-weight:700; letter-spacing:.3px}
    nav a{color:var(--muted); text-decoration:none; margin-left:1rem; padding:.4rem .6rem; border-radius:.6rem}
    nav a:hover{background:var(--bg-soft); color:var(--text)}

    main section{padding: 3rem 0; border-bottom:1px solid var(--border)}
    h1,h2{line-height:1.2; margin:0 0 .6rem}
    h1{font-size: clamp(1.6rem, 2.5vw + 1rem, 2.6rem)}
    h2{font-size: clamp(1.2rem, 2vw + .6rem, 1.6rem)}
    p{color:var(--muted)}
    .card{background:var(--card); border:1px solid var(--border); border-radius:1.2rem; padding:1rem}

    /* Projects grid */
    .grid{display:grid; gap:1rem; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));}

    /* Form + results */
    form label{display:block; font-weight:600; margin-top:.6rem}
    input, button{font:inherit}
    input[type="number"], input[type="text"], input[type="email"]{
      width:100%; box-sizing:border-box; padding:.65rem .7rem; margin-top:.3rem; border-radius:.7rem;
      background:#0d142a; color:var(--text); border: 1px solid var(--border);
    }
    .row{display:grid; grid-template-columns: 1fr 1fr; gap:.8rem}
    @media (max-width:640px){ .row{grid-template-columns:1fr} }
    button{
      display:inline-block; margin-top:1rem; padding:.7rem 1rem; border-radius:.8rem; cursor:pointer; border:1px solid var(--border);
      background:linear-gradient(135deg, var(--accent), var(--accent-2)); color:#0a0a0a; font-weight:700;
    }

    .result{margin-top:1rem; display:grid; grid-template-columns: 1fr 1fr; gap:.8rem}
    .result .box{background:#0d142a; border:1px solid var(--border); border-radius:.8rem; padding: .8rem}
    .result .label{color:var(--muted); font-size:.9rem}
    .result .value{font-size:1.3rem; font-weight:700}

    footer{padding:2rem 0; color:var(--muted)}
    .tag{display:inline-block; padding:.2rem .5rem; border:1px solid var(--border); border-radius:999px; font-size:.8rem; color:var(--muted)}
  </style>
</head>
<body>
  <header>
    <div class="container nav">
      <div class="logo">✈️ My Aerospace Portfolio</div>
      <nav>
        <a href="#about">About</a>
        <a href="#projects">Projects</a>
        <a href="#tools">Tools</a>
        <a href="#contact">Contact</a>
      </nav>
    </div>
  </header>

  <main class="container">
    <section id="about">
      <h1>Hi! I'm an aerospace engineer.</h1>
      <p>This is my personal website built with plain <strong>HTML</strong>, a bit of <strong>CSS</strong> for looks, and a sprinkle of <strong>JavaScript</strong> for interactivity. We'll use it to learn the web stack step-by-step and later add a backend (Python FastAPI or C# ASP.NET), plus deployment.</p>
      <div class="card">
        <span class="tag">Tech stack (v1)</span>
        <p>HTML · CSS · JavaScript (no frameworks yet)</p>
      </div>
    </section>

    <section id="projects">
      <h2>Projects</h2>
      <div class="grid">
        <article class="card">
          <h3>Wing Sizing Study</h3>
          <p>Parametric sweep of aspect ratio vs. induced drag. (Placeholder cards you can replace with real work.)</p>
        </article>
        <article class="card">
          <h3>Propeller BEMT</h3>
          <p>Simple BEMT analysis and visualization.</p>
        </article>
        <article class="card">
          <h3>CFD Post-Processing</h3>
          <p>Automated plots and report generation.</p>
        </article>
      </div>
    </section>

    <section id="tools">
      <h2>Mini Tool: Dynamic Pressure & Drag</h2>
      <p>Quick calculator to demo JavaScript interacting with the page. Edit values and see results update.</p>
      <form id="calc-form" class="card" onsubmit="event.preventDefault();">
        <div class="row">
          <div>
            <label for="speed">Speed V (m/s)</label>
            <input type="number" id="speed" step="any" value="70" />
          </div>
          <div>
            <label for="rho">Air density ρ (kg/m³)</label>
            <input type="number" id="rho" step="any" value="1.225" />
          </div>
          <div>
            <label for="area">Reference area A (m²)</label>
            <input type="number" id="area" step="any" value="0.7" />
          </div>
          <div>
            <label for="cd">Drag coefficient C<sub>d</sub></label>
            <input type="number" id="cd" step="any" value="0.32" />
          </div>
        </div>
        <button id="compute" type="button">Compute</button>
        <div class="result">
          <div class="box">
            <div class="label">Dynamic pressure q</div>
            <div class="value"><span id="q">—</span> Pa</div>
          </div>
          <div class="box">
            <div class="label">Drag D</div>
            <div class="value"><span id="drag">—</span> N</div>
          </div>
        </div>
      </form>
    </section>

    <section id="contact">
      <h2>Contact</h2>
      <form class="card" id="contact-form" onsubmit="event.preventDefault(); alert('In v2 we will send this to a backend!');">
        <label for="name">Name</label>
        <input id="name" type="text" placeholder="Your name" required />

        <label for="email">Email</label>
        <input id="email" type="email" placeholder="you@example.com" required />

        <label for="msg">Message</label>
        <input id="msg" type="text" placeholder="Say hi..." required />

        <button type="submit">Send</button>
      </form>
    </section>
  </main>

  <footer>
    <div class="container">© <span id="year"></span> My Name. Built from scratch.</div>
  </footer>

  <script>
    // Utility: format numbers nicely
    const fmt = (x) => Number.isFinite(x) ? x.toLocaleString(undefined, {maximumFractionDigits: 3}) : '—';

    function compute(){
      const V   = parseFloat(document.querySelector('#speed').value);
      const rho = parseFloat(document.querySelector('#rho').value);
      const A   = parseFloat(document.querySelector('#area').value);
      const Cd  = parseFloat(document.querySelector('#cd').value);

      // q = 0.5 * rho * V^2
      const q = 0.5 * rho * V * V;
      // D = q * Cd * A
      const D = q * Cd * A;

      document.querySelector('#q').textContent = fmt(q);
      document.querySelector('#drag').textContent = fmt(D);
      // Persist last values so they survive refreshes
      localStorage.setItem('calc-state', JSON.stringify({V, rho, A, Cd}));
    }

    // Hook up events
    document.querySelector('#compute').addEventListener('click', compute);
    ['#speed','#rho','#area','#cd'].forEach(sel => document.querySelector(sel).addEventListener('input', compute));

    // Restore last state if present
    try{
      const saved = JSON.parse(localStorage.getItem('calc-state'));
      if(saved){
        document.querySelector('#speed').value = saved.V;
        document.querySelector('#rho').value   = saved.rho;
        document.querySelector('#area').value  = saved.A;
        document.querySelector('#cd').value    = saved.Cd;
      }
    }catch{}

    // Set current year and do an initial compute
    document.querySelector('#year').textContent = new Date().getFullYear();
    compute();
  </script>
</body>
</html>
